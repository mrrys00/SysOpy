s1 = rev
s2 = head -n 1 /etc/passwd
s3 = head -c 7
 comment (after space)
#comment (after #)
s2 # comment

y6 = head -c 3 # an instruction can be added here, but isn't available above this line

s2 | s1 | s3 | s1 | y6 | s1
# read, reverse, cut, reverse, cut, reverse

skladnik2 = ls | grep '^i'
skladnik2
